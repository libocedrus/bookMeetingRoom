{
  "name": "book-meeting-room",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "generate": "node github-pages/scripts/generate-data.js",
    "serve": "node scripts/local-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
